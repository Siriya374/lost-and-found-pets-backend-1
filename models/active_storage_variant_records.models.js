class active_storage_variant_records {
  constructor(
    id,
    blob_id,
    variation_digest,
  ) {
    this.id = id;
    this.blob_id = blob_id;
    this.variation_digest = variation_digest;
  }
}

export default active_storage_variant_records;
